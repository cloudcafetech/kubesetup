apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: loki-gateway
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
    - host: loki.3.16.154.209.nip.io
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: loki-gateway
                port:
                  number: 80
